import type { Metadata } from "next";
import { Geist, Geist_Mono, Lato } from "next/font/google";
import "./globals.css";
import Providers from "./Providers";
import NavbarClient from "../component";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const lato = Lato({
  subsets: ["latin"],
  weight: ["300", "400", "700", "900"],
  variable: "--font-lato",
  display: "swap",
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Lumora Bhutan",
  description: "Generated by Lumora Bhutan",
  icons: {
    icon: [
      { url: "/favicon-96x96.png", sizes: "96x96", type: "image/png" },
    ],
    apple: "/apple-touch-icon.png",
    shortcut: "/favicon.ico",
  },
  openGraph: {
    title: "Lumora Bhutan",
    description: "Generated by Lumora Bhutan",
    url: "https://lumorabhutan.com",
    siteName: "Lumora Bhutan",
    images: [
      {
        url: "https://lumorabhutan.com/logo.png",
        width: 1200,
        height: 630,
        alt: "Lumora Bhutan Logo",
      },
    ],
    locale: "en_US",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  const jsonLd = {
    "@context": "https://schema.org",
    "@type": "Organization",
    name: "Lumora Bhutan",
    url: "https://lumorabhutan.com",
    logo: "https://lumorabhutan.com/logo.png",
  };

  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} ${lato.variable} antialiased relative`}
      >
        {/* Move JSON-LD script to body */}
        <script
          type="application/ld+json"
          dangerouslySetInnerHTML={{
            __html: JSON.stringify(jsonLd),
          }}
        />
        
        <Providers>
          <NavbarClient />
          {children}
        </Providers>
      </body>
    </html>
  );
}